#!/bin/bash

if [[ -z "$PORT" ]]; then
  PORT=9393
fi

if ! vagrant status | grep "running" > /dev/null; then
  vagrant up
fi

vagrant ssh -c "cd /vagrant; PATH=/home/vagrant/npm/bin:$PATH GEM_PATH=/home/vagrant/.gems shotgun -p $PORT"
